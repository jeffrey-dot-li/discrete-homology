[package]
name = "discrete-homology"
version = "0.1.0"
edition = "2021"

[dependencies]


[package.metadata.docs.rs]
features = ["nightly"]


[features]
default = []
nightly = []

[dev-dependencies]
criterion = "0.8.1"

[[bench]]
name = "maps_naive"
harness = false
